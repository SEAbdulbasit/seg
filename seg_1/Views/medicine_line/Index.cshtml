@model IEnumerable<seg_1.Models.medicine_line>

@{
    ViewBag.Title = "Index Medicinne Line";
}

<h2>Index Medicine Line</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.medicine.medicine_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.sales.sales_id)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.medicine.medicine_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.sales.sales_id)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.medicine_line_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.medicine_line_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.medicine_line_id })
        </td>
    </tr>
}

</table>
