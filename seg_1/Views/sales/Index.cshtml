@model IEnumerable<seg_1.Models.sales>

@{
    ViewBag.Title = "Index";
}

<h2>Index Sales</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.salesman.salesman_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.sales_date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.sales_total_amount)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.salesman.salesman_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.sales_date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.sales_total_amount)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.sales_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.sales_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.sales_id })
        </td>
    </tr>
}

</table>
